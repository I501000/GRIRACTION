'use strict';

var execSync = require('child_process').execSync;
var _ = require('lodash');

module.exports = function shell(cmd, options) {
  console.log('>', cmd);
  execSync(cmd, _.merge({
    stdio: 'inherit',
    env: _.clone(process.env)
  }, options));
}
